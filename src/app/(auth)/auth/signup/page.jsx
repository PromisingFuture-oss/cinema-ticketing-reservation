import React from 'react'
import SignupForm from '../components/SignupForm'

export const metadata = {
    title: "Signup"
};

const page = () => {
    return (
        <SignupForm />
    )
}

export default page
